import { FC } from 'react';

import { Game2 } from '@/components/Game2';

const page: FC = ({}) => {
  return (
    <main className="grid place-items-center flex-1">
      <Game2 />
    </main>
  );
};

export default page;
